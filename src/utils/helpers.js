/**
 * Split spaced character string (eg. fullname)
 *
 * @param {string} param - The name param to split
 * @returns {Array} A list of string values
 */
export const splitName = (param) => param.toString().split(' ');

export default {};
